# 💌 Love Poll с отправкой на почту

Проект с опросом, который отправляет ответы на e-mail.

## Структура файлов

```
love/
├── index.html          # Основная страница (фронтенд)
├── style.css           # Стили
├── server.js           # Node.js сервер (отправка писем)
├── package.json        # Зависимости
├── .env.example        # Шаблон переменных окружения
└── README.md           # Этот файл
```

## Установка и запуск

### 1. Установи зависимости

```bash
npm install
```

### 2. Настрой переменные окружения

**Скопируй `.env.example` в `.env`:**
```bash
cp .env.example .env
```

**Заполни `.env` со своими данными для отправки писем:**

#### Вариант A: Gmail (рекомендуется)
```
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
MAIL_USER=your-gmail@gmail.com
MAIL_PASSWORD=your-app-password
```

Как создать App Password в Gmail:
1. Перейди на [myaccount.google.com/apppasswords](https://myaccount.google.com/apppasswords)
2. Выбери "Mail" и "Windows Computer"
3. Google создаст 16-символьный пароль
4. Скопируй пароль в `MAIL_PASSWORD`

#### Вариант B: iCloud Mail
```
SMTP_HOST=smtp.mail.icloud.com
SMTP_PORT=587
MAIL_USER=your-email@icloud.com
MAIL_PASSWORD=your-app-specific-password
```

Как создать App Password в iCloud:
1. Перейди на [appleid.apple.com/account/manage](https://appleid.apple.com/account/manage)
2. Security → App-specific passwords
3. Создай пароль для Mail
4. Скопируй в `MAIL_PASSWORD`

### 3. Запусти сервер

```bash
npm start
```

В консоли увидишь:
```
🚀 Сервер запущен на http://localhost:3000
Готовность к отправке писем...
```

### 4. Открой в браузере

```
http://localhost:3000
```

Заполни опрос и голосуй — ответ придёт на `yernazaraltynbekov@icloud.com` 📧

## Как работает

1. 🎯 Пользователь выбирает ответ ( Да / Нет )
2. 💬 Может написать комментарий (необязательно)
3. ✅ Клик на "Голосовать"
4. 📨 Сервер отправляет оформленное письмо на почту
5. 😊 Пользователю показывается "спасибо"

## Настройка почты получателя

В `server.js` строка:
```javascript
to: 'yernazaraltynbekov@icloud.com',
```

Если нужна другая почта— просто измени эту строку.

## Решение проблем

**Письма не приходят?**
- Проверь, что `.env` заполнена правильно
- Убедись, что приложение может доступ к интернету
- Для Gmail: проверь App Passwords настройки
- Посмотри консоль сервера на ошибки

**Хочу использовать другой SMTP?**
- Добавь в `.env`:
  ```
  SMTP_HOST=smtp.your-provider.com
  SMTP_PORT=587
  MAIL_USER=your-email@your-provider.com
  MAIL_PASSWORD=your-password
  ```

---

Сделано с 💖
